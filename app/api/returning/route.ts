import { NextResponse } from 'next/server'; import { returningSchema } from '@/lib/schemas'; import { prisma } from '@/lib/prisma';
export async function POST(req:Request){try{const d=returningSchema.parse(await req.json()); await prisma.guest.create({data:{firstName:d.firstName,lastName:d.lastName,phone:d.phoneOrEmail,email:d.phoneOrEmail,firstTimeVisitor:false,adultsCount:1,childrenCount:0,wantsContact:false,permissionToContact:false,serviceAttending:d.serviceAttending,prayerRequests:d.prayerRequest?{create:[{request:d.prayerRequest}]}:undefined,notes:d.updates?{create:[{note:d.updates,status:'UPDATE'}]}:undefined}}); return NextResponse.json({ok:true});}catch{return NextResponse.json({error:'Invalid submission'},{status:400});}}
