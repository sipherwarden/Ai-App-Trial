import { prisma } from '@/lib/prisma'; import bcrypt from 'bcryptjs'; import { setAdminSession } from '@/lib/auth'; import { redirect } from 'next/navigation';
async function login(fd:FormData){'use server';const email=String(fd.get('email')||'');const password=String(fd.get('password')||'');const u=await prisma.adminUser.findUnique({where:{email}});if(!u||!(await bcrypt.compare(password,u.passwordHash))) return; await setAdminSession(u.id); redirect('/admin');}
export default function Page(){return <form action={login} className='max-w-md mx-auto mt-20 card space-y-3'><h1 className='text-2xl font-bold'>Admin Login</h1><input name='email' className='w-full border p-2 rounded' required/><input type='password' name='password' className='w-full border p-2 rounded' required/><button className='btn-primary'>Login</button></form>}
