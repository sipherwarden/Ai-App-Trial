import Link from 'next/link'; import { prisma } from '@/lib/prisma'; import { requireAdmin } from '@/lib/auth';
export default async function Page({searchParams}:{searchParams:any}){await requireAdmin(); const q=searchParams?.q||''; const guests=await prisma.guest.findMany({where:q?{OR:[{firstName:{contains:q}},{lastName:{contains:q}},{email:{contains:q}},{phone:{contains:q}},{serviceAttending:{contains:q}}]}:undefined,orderBy:{createdAt:'desc'},take:100}); return <main className='p-6'><h1 className='text-3xl mb-4'>Guest Management</h1><form><input name='q' defaultValue={q} placeholder='Search name, phone, email, date, service' className='border p-2 rounded w-full max-w-md'/></form><table className='w-full mt-4 bg-white'><thead><tr><th>Name</th><th>Service</th><th>Follow-up</th></tr></thead><tbody>{guests.map(g=><tr key={g.id}><td><Link href={`/admin/guests/${g.id}`}>{g.firstName} {g.lastName}</Link></td><td>{g.serviceAttending}</td><td>{g.followUpStatus}</td></tr>)}</tbody></table><Link href='/api/admin/guests/export' className='btn-secondary mt-4'>Export CSV</Link></main>}
